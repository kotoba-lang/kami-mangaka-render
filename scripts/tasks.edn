;; Originally generated from bb.edn (ADR-2607173000 Wave 3); repaired 2026-08-13
;; (ADR-2608131600).  nbb scripts/run-task.cljs <task> [args…]
;;
;; The Wave-3 conversion emitted an EMPTY registry here and deleted bb.edn, so
;; this repo had no runnable entrypoint at all from 2026-07-17 until today.
;; bb.edn's sole task was `(p/shell "clojure -M:test")`; the converter matched
;; only the bare symbol `shell`, not the `p/` alias, and filed a one-line
;; conversion as a hand-port backlog item (scripts/tasks-complex.edn).
{:test {:cmd ["clojure" "-M:test"]
        :doc "Run the render-commons unit tests (pure: compose / budget / dims)"}}
